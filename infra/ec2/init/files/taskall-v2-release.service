[Unit]
Description=taskall-v2 release check (S3 polling)
# 排他制御はrelease.sh内のflockで行うため、systemd側では特別な設定はしない

[Service]
Type=oneshot
# release.sh自身を含むEC2側スクリプト一式を、実行のたびにS3から最新化してから実行する(issue #54)
ExecStartPre=/opt/taskall-v2/bin/update-ec2-scripts.sh
ExecStart=/opt/taskall-v2/bin/release.sh
